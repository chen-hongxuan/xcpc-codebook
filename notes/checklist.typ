== 赛前五分钟检查

- 确认图是否有向，是否允许重边和自环。
- 先估计上界，再选择整数类型。
- 测试最小输入、空结构和全部相等的情形。
- 最后一次提交前重新阅读输出要求。

== 常用常数

#table(
  columns: (1fr, 1fr),
  stroke: 0.35pt + luma(150),
  inset: 2pt,
  [常数], [数值],
  [$pi$], [$3.141592653589793$],
  [$sqrt(2)$], [$1.414213562373095$],
  [$ln 2$], [$0.693147180559945$],
)
