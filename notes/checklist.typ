== 赛前五分钟检查

- 临时关闭终端内存限制: ulimit -s unlimited
- 永久关闭终端内存限制:
  1. 打开 `~/.bashrc`
  2. 在配置文件末尾添加 `ulimit -s unlimited`
  3. 保存退出后在终端运行 `source ~/.bashrc`
- 十年oi一场空, 不开longlong见祖宗
- 多测不清空, 亲人两行泪

== 常用常数

#table(
  columns: (1fr, 1fr),
  stroke: 0.35pt + luma(150),
  inset: 2pt,
  [常数], [数值],
  [$pi$ ], [$3.141592653589793$ ],
  [$sqrt(2)$ ], [$1.414213562373095$ ],
  [$ln 2$ ], [$0.693147180559945$ ],
)

=== 整数因子数

#h(2em) 记 $d(n)$ 为 $n$ 的正因子个数, $omega(n)$ 为 $n$ 的本质不同质因子个数. 下表给出精确最大值, 因而也是对 $n<=N$ 的上界.

#table(
  columns: (1fr, 1.4fr, 1.4fr),
  stroke: 0.35pt + luma(150),
  inset: 2pt,
  [$N$], [$max_(1<=n<=N)d(n)$], [$max_(1<=n<=N)omega(n)$],
  [$10^3$], [$32$], [$4$],
  [$10^5$], [$128$], [$6$],
  [$10^6$], [$240$], [$7$],
  [$10^7$], [$448$], [$8$],
  [$10^9$], [$1344$], [$9$],
  [$10^12$], [$6720$], [$11$],
  [$10^18$], [$103680$], [$15$],
)

=== 常用对数

#h(2em) 下表保留三位小数.

#table(
  columns: (1fr, 1fr, 1fr),
  stroke: 0.35pt + luma(150),
  inset: 2pt,
  [$n$], [$log_2 n$], [$ln n$],
  [$10^3$], [$9.966$], [$6.908$],
  [$10^5$], [$16.610$], [$11.513$],
  [$10^6$], [$19.932$], [$13.816$],
  [$10^7$], [$23.253$], [$16.118$],
  [$10^9$], [$29.897$], [$20.723$],
  [$10^12$], [$39.863$], [$27.631$],
  [$10^18$], [$59.795$], [$41.447$],
)

=== 整除对数量

#h(2em) 记 $H(n)=sum_(i=1)^n floor(n/i)$ , 即满足 $1<=i<=j<=n$ 且 $i$ 是 $j$ 的因子的数对 $(i,j)$ 个数. $H(N)$ 是对任意 $n<=N$ 的上界.

#table(
  columns: (1fr, 1fr),
  stroke: 0.35pt + luma(150),
  inset: 2pt,
  [$N$], [$H(N)$],
  [$10^3$], [$7069$],
  [$10^5$], [$1166750$],
  [$10^6$], [$13970034$],
  [$10^7$], [$162725364$],
  [$10^9$], [#raw("\\")],
  [$10^12$], [#raw("\\")],
  [$10^18$], [#raw("\\")],
)

=== 素数间隔

#h(2em) 对相邻素数 $p_k,p_(k+1)$ , 记 $G(N)=max_(p_k<=N)(p_(k+1)-p_k)$ . 下表给出已经穷举验证的精确最大值, 因而可作为 $p_k<=N$ 时到下一素数的距离上界. 间隔指两端素数之差, 中间的合数个数比它少 $1$ .

#table(
  columns: (1fr, 1fr),
  stroke: 0.35pt + luma(150),
  inset: 2pt,
  [$N$], [$G(N)$],
  [$10^3$], [$20$],
  [$10^5$], [$72$],
  [$10^6$], [$114$],
  [$10^7$], [$154$],
  [$10^9$], [$282$],
  [$10^12$], [$540$],
  [$10^18$], [$1442$],
)
