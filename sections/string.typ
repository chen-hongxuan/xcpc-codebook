#import "../common.typ": *

= 字符串 / String

== 哈希类(三哈希)
#code-info(
  [`HASH(x)`/`HASH(x,y,z)` 构造三元哈希; `[]` 访问分量; `+=,-=,*=` 及对应二元运算逐模计算; `==,!=,<` 比较三元组. 使用前须替换 `MOD` 与 `BASE`, 并保证构造值、`BASE` 及经 `[]` 写入的值均在对应的 `[0,MOD)` 内. ],
  [模数个数固定为 $3$ , 所有接口的时间与额外空间均为 $O(1)$ ; 乘法中间积须能由 `long long` 表示. ],
)
#code-file("code/string/hash.cpp")

=== 常用哈希参数

#text(size: 6pt)[
  #table(
    columns: (0.8fr, 1.15fr, 1.45fr, 1.6fr),
    stroke: 0.35pt + luma(150),
    inset: 1.5pt,
    align: center,
    [*方案*], [*BASE*], [*MOD*], [*说明*],
    [三哈希 1], [`1000003`], [`998244353`], [可直接填入第 $1$ 组],
    [三哈希 2], [`1000033`], [`1000000007`], [可直接填入第 $2$ 组],
    [三哈希 3], [`1000037`], [`1000000009`], [可直接填入第 $3$ 组],
    [经典小底数], [`131`], [`1000000007`], [短字符串常用],
    [经典小底数], [`13331`], [`1000000009`], [短字符串常用],
    [常见大底数], [`911382323`], [`972663749`], [二者均为质数],
    [61 位 Mersenne], [$[256,2^32)$ 中随机], [$2^61-1$], [须使用 `__int128` 或专门约减],
  )
]

#h(2em) 推荐三哈希直接取表中前三组, 即 `BASE={1000003,1000033,1000037}` 与 `MOD={998244353,1000000007,1000000009}` . 这些约 $10^9$ 的模数可安全使用当前 `long long` 乘法; $2^61-1=2305843009213693951$ 虽然也是常用质数, 但直接相乘会溢出当前实现.

#h(2em) 底数实际上不要求为质数, 只须满足 $1<"BASE"<"MOD"$ , 通常还应大于字符编码范围. 面对可构造数据时, 比起长期固定表中组合, 更稳妥的做法是每次运行独立随机选择非 $0,1$ 的底数.

== KMP & exKMP
=== 代码
#code-info(
  [`KMP(x,pi)` 求失配数组(最长真 border 的末下标); `exKMP(x,z)` 求各后缀与原串的 LCP, 约定 `pi[0]=-1,z[0]=0`. ],
  [字符串长度为 $n$ 时, 两者均为 $O(n)$ 时间、$O(n)$ 输出空间, 除此之外额外空间 $O(1)$ . ],
)
#code-file("code/string/kmp.cpp")

=== 核心性质

#h(2em) `pi[i]` 记录前缀 `S[0..i]` 的最长真 border 末下标. KMP 失配时沿 `pi` 跳到次长候选, 候选长度严格减小; 指针在整个扫描过程中总共只前进与回退 $O(n)$ 次.

#h(2em) 对 exKMP, $Z_i="z"[i]$ 是 $S$ 与后缀 `S[i..]` 的 LCP 长度. 算法维护当前右端点最大的匹配区间, 区间内先复用已知 $Z$ 值, 必要时再暴力扩展, 因此总时间为 $O(n)$ . 本实现按方便递推的约定令 `z[0]=0`; 数学上也常约定 $Z_0=|S|$ .

=== 单模式匹配

#h(2em) 先对模式串 $P$ 求 `pi`, 再从左到右扫描文本 $T$ 并维护已经匹配到的模式串末下标 $j$ . 失配时反复令 `j=pi[j]`, 匹配成功则递增 $j$; 当 $j=|P|-1$ 时得到一次出现, 随后令 `j=pi[j]` 以继续寻找允许重叠的匹配. 总时间为 $O(|P|+|T|)$ , 额外空间为 $O(|P|)$ .

#h(2em) 也可对 `P + 分隔符 + T` 求 exKMP. 文本位置 $i$ 匹配成功, 当且仅当相应位置的 $Z$ 值至少为 $|P|$ . 更一般地, 这些 $Z$ 值直接给出 $P$ 与每个文本后缀的 LCP, 可用于定位首个失配位置及比较固定模式串与各后缀.

== Manacher

#code-info(
  [`work(str,d)` 在原串字符之间及两端隐式插入 `#` , 并求出变换串的回文半径. `d[i]` 是以 $i$ 为中心且包含中心的最大回文半径, 对应区间为 $[i-d_i+1,i+d_i-1]$ . 原串的最长回文子串长度为 $max_i d_i-1$ . ],
  [时间为 $O(n)$ , 输出及临时空间均为 $O(n)$ . `d` 的长度为 $2n+1$ , 原串不能包含作为分隔符的 `#` . ],
)
#code-file("code/string/manacher.cpp")


== AC自动机
=== 代码
#code-info(
  [`init()` 清空; `insert(str)` 插入小写模式串并返回终点; `construct()` 构造 fail 指针并补全转移; `cnt[p]` 记录以 $p$ 结尾的模式串数. 须依次执行 `init()`、若干次 `insert()`、一次 `construct()`, 重新构建前再次初始化. ],
  [设模式总长为 $L$ 、状态数为 $P$ : 插入总计 $O(L)$ , 构造 $O(P)$ , 空间 $O(P)$ . ],
)
#code-file("code/string/acam.cpp")

=== 核心性质

#h(2em) 每个 Trie 节点 $v$ 表示根到 $v$ 的字符串 $S_v$ . `fail[v]` 指向 $S_v$ 的最长真后缀, 且该后缀也是某个 Trie 前缀. `construct()` 补全转移后, `trie[v][c]` 直接表示 $S_v+c$ 的最长 Trie 后缀, 因而扫描文本时每个字符只需一次转移.

#h(2em) 将 `fail` 边反向得到 fail 树. 文本扫描到状态 $v$ 时, 当前结尾能够匹配的模式串恰好是 $v$ 及其 fail 链上所有终止节点. `cnt[v]` 只记录恰好结束在 $v$ 的模式串数量, 相同模式串会在同一节点累加.

=== 多模式匹配与计数

#h(2em) 插入时保存每个模式串的终止节点. 扫描文本, 每到达状态 $v$ 就令访问次数 $V_v$ 加一; 构造时另存 BFS 序, 最后按 BFS 逆序执行 $V_("fail"[v])<-V_("fail"[v])+V_v$ . 此时某模式串的出现次数就是其终止节点的 $V$ 值, 总时间为模式总长、文本长度与状态数之和.

#h(2em) 若只需统计文本中所有匹配的总次数, 可按 BFS 序预处理 $O_v="cnt"[v]+O_("fail"[v])$ , 扫描时累加当前状态的 $O_v$ . 若要报告每个具体出现位置, 则沿输出链接枚举终止节点; 复杂度还需加上实际输出数量.

=== 自动机上的 DP 与最短路

#h(2em) 按 BFS 序标记 $"bad"[v]=("cnt"[v]>0) or "bad"["fail"[v]]$ , 即可在非 `bad` 状态上进行 DP 或矩阵快速幂, 统计固定长度且不包含任一模式串的字符串. 将终止模式编号压入状态掩码并沿 `fail` 继承, 还可在 `(状态,掩码)` 上 BFS 或 DP, 求包含全部模式串的最短串、计数或最小代价串.


== 后缀自动机
=== 代码
#code-info(
  [`sam_init()` 重置; `extend(c)` 追加小写字符; `construct(str)` 重建 SAM 并返回整串对应的 `last` 状态. ],
  [长度为 $n$ 的整串构造总时间、状态数与空间均为 $O(n)$ ; 单次 `extend` 最坏 $O(n)$ 、序列上均摊 $O(1)$ . ],
)
#code-file("code/string/sam.cpp")

=== 核心性质

#h(2em) 记 $L_v$ 与 $F_v$ 分别为 `st[v].len` 与 `st[v].fail` , $E_v$ 为状态 $v$ 所表示子串的结束位置集合. SAM 将 $E_v$ 相同的子串合并为一个状态, 并满足:

- 对 $v!=0$ , 状态 $v$ 恰好表示长度位于 $[L_(F_v)+1,L_v]$ 的那些后缀, 每个长度对应一个子串.
- 每条转移边均由较短状态指向较长状态, 因此转移图是 DAG; 从根出发的每条非空路径唯一对应一个本质不同子串.
- $E_v subset.eq E_(F_v)$ . 将 `fail` 边反向即得 parent 为 $F_v$ 的后缀链接树, $v$ 的子树汇集了 $E_v$ 中的结束位置.
- 从 `last` 不断沿 `fail` 到根所经过的状态, 恰好表示原串的所有后缀所属状态.

=== 本质不同字串数量

#h(2em) 每个非根状态 $v$ 贡献 $L_v-L_(F_v)$ 个新子串, 因此本质不同子串数量与它们的长度之和分别为
$
  sum_(v!=0)(L_v-L_(F_v)),
  quad sum_(v!=0) frac((L_(F_v)+1+L_v)(L_v-L_(F_v)),2).
$
在线逐字符 `extend` 时, 令 $v$ 为新的 `last` , 第一个量直接增加 $L_v-L_(F_v)$ .

=== 出现次数与重复子串

#h(2em) 为节点增加 $C_v$ : 每次追加字符产生的普通状态将其置为 $1$ , 克隆状态将其置为 $0$ . 建完后将状态按 $L_v$ 降序处理, 执行 $C_(F_v) <- C_(F_v)+C_v$ , 最终 $C_v=|E_v|$ . 模式串从根沿转移走到 $v$ 后, 它的出现次数就是 $C_v$ .

#h(2em) 出现次数恰为 $k$ 的本质不同子串数量, 等于对所有满足 $C_v=k$ 的状态累加 $L_v-L_(F_v)$ ; 将条件改成 $C_v>=k$ 即得出现至少 $k$ 次的数量. 出现至少 $k$ 次的最长子串长度为这些状态中最大的 $L_v$ .

=== 字典序第 $k$ 小子串

#h(2em) 在转移 DAG 上逆拓扑计算 $D_v=1+sum_(v->u)D_u$ , 其中 $1$ 表示在当前状态停止, 根的空串不计, 所以总数为 $D_0-1$ . 从根开始按 `a-z` 枚举转移 $v->u$ : 该边对应连续的 $D_u$ 个答案; 若 $k>D_u$ 则减去它, 否则输出该字符并进入 $u$ , 先令 $k<-k-1$ 以跳过当前串, 再继续枚举. 若按出现次数计重, 将非根状态的停止权 $1$ 改为 $C_v$ , 根的停止权改为 $0$ .

=== 最长公共子串

#h(2em) 对第一个串建 SAM, 再扫描第二个串并维护当前状态 $p$ 与匹配长度 $ell$ . 有当前字符的转移就前进并令 $ell<-ell+1$ ; 否则沿 `fail` 跳转, 每次把 $ell$ 截断到新状态的 $L_p$ , 直到存在转移; 根也不存在转移时令 $p=0,ell=0$ . 扫描中最大的 $ell$ 即为答案, 总时间为两串长度之和.

#h(2em) 对多个串, 每扫描一个新串都记录到达各状态时的最大匹配长度 $M_v$ , 再按 $L_v$ 降序执行 $M_(F_v) <- max(M_(F_v),min(M_v,L_(F_v)))$ . 令 $A_v$ 初始为 $L_v$ , 每轮执行 $A_v<-min(A_v,M_v)$ , 最终答案为 $max_v A_v$ .

=== 最短未出现串

#h(2em) 固定字符集后, 在转移 DAG 上按 $L_v$ 降序计算 $Q_v$ : 若 $v$ 存在缺失的字符转移则 $Q_v=1$ , 否则 $Q_v=1+min_(v->u)Q_u$ . $Q_0$ 即最短未出现子串的长度; 每步优先选择满足递推式的最小字符即可恢复字典序最小的答案. 时间为 $O(26P)$ , 其中 $P$ 为状态数.

== 回文自动机

=== 代码

#code-info(
  [`pam_init()` 重置; `extend(c)` 追加一个小写字符; `construct(str)` 重建 PAM 并返回整串对应的 `last` 状态. `st[p].len/fail` 分别记录节点回文串长度与最长真回文后缀, `last` 表示当前串的最长回文后缀. 除两个根外, 每个节点对应一个本质不同的回文子串. ],
  [长度为 $n$ 的整串构造时间为 $O(n)$ , 状态数不超过 $n+2$ , 空间为 $O(26n)$ ; 单次 `extend` 最坏为 $O(n)$ . 字符必须位于 `a-z` ; 将 `*` 视作通配符时, 每次追加可能产生多个新回文串, 不能直接套用本模板. ],
)
#code-file("code/string/pam.cpp")

=== 核心性质

#h(2em) PAM 有长度为 $0$ 与 $-1$ 的两个根, 统一处理偶回文与奇回文. 除根外, 每个节点唯一对应一个本质不同的回文子串; 从节点 $p$ 经字符 $c$ 的转移到 $v$ , 表示在 $p$ 对应回文的两端各加入一个 $c$ . `fail[v]` 指向 $v$ 的最长真回文后缀.

#h(2em) 每次 `extend` 后, `last` 是当前整个前缀的最长回文后缀, 沿 `fail` 链恰可枚举它的全部回文后缀. 每追加一个字符至多产生一个新的本质不同回文串, 因此长度为 $n$ 的字符串至多有 $n$ 个本质不同回文子串, PAM 至多有 $n+2$ 个节点.

=== 回文串计数

#h(2em) 本质不同回文子串数为非根节点数. 若为节点增加出现次数 $C_v$ , 每次追加字符后令 $C_("last")$ 加一, 建完后按回文长度从大到小执行 $C_("fail"[v])<-C_("fail"[v])+C_v$ , 则 $C_v$ 是节点回文在原串中的出现次数. 所有非根节点的 $C_v$ 之和是按位置计数的回文子串总数, 最大的节点长度是最长回文子串长度.

=== 回文后缀等差数列与回文划分

#h(2em) 在处理到位置 $i$ 时, 从 `last` 沿 `fail` 链恰可枚举所有以 $i$ 结尾的回文串, 但链长最坏为 $Theta(n)$ . 记 $L_v="len"[v],F_v="fail"[v]$ , 并定义
$
  "diff"[v]=L_v-L_(F_v).
$
若连续若干个 `fail` 节点具有相同的 `diff=d` , 则其长度依次相差 $d$ , 构成一个等差数列, 称为一个 series.

#h(2em) 定义 `series[v]=(diff[v]==diff[fail[v]]?series[fail[v]]:fail[v])` , 它直接跳到下一组. 从 `last` 开始反复令 `v=series[v]` , 每次恰访问一个 series 的代表. 可证明下一组的最长长度不超过当前代表长度的一半, 因而任意前缀的全部回文后缀至多分为 $O(log n)$ 个等差数列; 注意这并不表示 `fail` 链只有 $O(log n)$ 个节点.

#h(2em) 例如令 $D_i$ 为前 $i$ 个字符的最少回文分段数, 则
$
  D_i=min_v {D_(i-L_v)+1},
$
其中 $v$ 枚举当前前缀的非空回文后缀. 直接枚举 `fail` 链总计最坏 $O(n^2)$ ; 利用 series link 并缓存每组对应位置的最优值, 可将最少回文划分等能够按组维护贡献的 DP 优化至总计 $O(n log n)$ .

== 后缀排序

=== 代码

#code-info(
  [`work(s,sa,opt)` 将第 $i$ 小后缀的起点写入 `sa[i]`; `opt=0` 时 `rk[0]` 为最终后缀排名, `opt!=0` 时 `rk[i][j]` 为从 $j$ 开始、长度至多 $2^i$ 的前缀排名, 越过串尾处补最小哨兵. 要求字符串仅含小写字母. ],
  [时间 $O(n log^2 n)$ ; `opt=0` 空间 $O(n)$ , `opt!=0` 空间 $O(n log n)$ . ],
)
#code-file("code/string/sa.cpp")

=== 核心性质

#h(2em) `sa[r]` 是排名为 $r$ 的后缀起点, 逆数组 `rank[i]` 是后缀 `s[i..]` 的排名. 定义 $H_r$ 为 `s[sa[r-1]..]` 与 `s[sa[r]..]` 的 LCP, 并令 $H_0=0$ . 若两个后缀的排名为 $x<y$ , 则它们的 LCP 为
$
  min_(x<r<=y) H_r.
$
因此对 $H$ 建 RMQ 后可快速查询任意两个后缀的 LCP. $H$ 可用 Kasai 算法在线性时间求出: 按原串位置枚举后缀, 利用删除首字符后 LCP 至少减少一的性质复用上一次答案.

#h(2em) 本模板在 `opt!=0` 时保留每轮倍增排名. 对两个等长且长度为 $m$ 的子串, 令 $k=floor(log_2 m)$ , 分别比较它们开头与结尾的两个长度 $2^k$ 的块排名, 即可在 $O(1)$ 时间判断大小; 也可从大到小尝试相等块, 在 $O(log n)$ 时间求两个后缀的 LCP.

=== 子串计数与字典序查询

#h(2em) 排名为 $r$ 的后缀共有 $n-"sa"[r]$ 个非空前缀, 其中前 $H_r$ 个已在更小后缀中出现. 因而本质不同子串数量为
$
  frac(n(n+1),2)-sum_(r=1)^(n-1)H_r.
$
按 `sa` 顺序扫描时, 该后缀依次贡献长度 $H_r+1$ 至 $n-"sa"[r]$ 的新子串; 用贡献数量定位后即可求字典序第 $k$ 小的本质不同子串.

=== 重复子串与公共子串

#h(2em) 最长重复子串长度为 $max_r H_r$ . 当 $k>=2$ 时, 出现至少 $k$ 次的最长子串对应连续 $k$ 个后缀: 对每个 `sa[l..l+k-1]` 取 $H_(l+1),dots,H_(l+k-1)$ 的最小值, 再对所有窗口取最大值; 滑动窗口最小值可在线性时间完成.

#h(2em) 求两个串的最长公共子串时, 用未在原串出现的分隔符拼接后排序, 答案是来自不同原串的相邻后缀之间最大的 $H_r$ . 多串情形使用互异分隔符并忽略从分隔符开始的后缀, 再用双指针维护一个覆盖所有原串的后缀排名区间, 同时维护区间内 $H$ 的最小值.

=== 模式匹配

#h(2em) 与模式串 $P$ 具有前缀关系的后缀在 `sa` 中形成连续区间. 用两次二分分别找到第一个不小于 $P$ 的后缀与第一个严格大于所有以 $P$ 为前缀的后缀, 区间长度就是出现次数, 区间中的 `sa` 值就是所有出现位置. 直接比较的复杂度为 $O(|P|log n)$ ; 配合 LCP 可减少重复字符比较.

== Border 理论

=== 定义与 Border 链

#h(2em) 设字符串 $S$ 的长度为 $n$ . 若 $1<=k<n$ 且 `S[0..k-1]` 等于 `S[n-k..n-1]` , 则长度 $k$ 是 $S$ 的一个真 border. 即 border 同时是原串的前缀与后缀; 讨论周期时常把长度 $0$ 的空串也加入 border 集合. 例如 `ababab` 的真 border 长度为 $2,4$ .

#h(2em) 令 $B_k$ 为长度 $k$ 的前缀的最长真 border 长度, 并令 $B_0=0$ . 本文 KMP 的下标约定给出 $B_k="pi"[k-1]+1$ . 若 $b$ 是一个 border, 则 $b$ 的任意 border 仍是原串的 border; 因而长度 $k$ 的前缀的全部非空 border 可从 $b=B_k$ 开始, 反复令 $b<-B_b$ 枚举. 这也是 KMP 失配转移的结构基础.

=== Border 树

#h(2em) 建立编号为 $0,1,dots,n$ 的节点, 节点 $k$ 表示长度 $k$ 的前缀, 对 $k>0$ 连边 $B_k -> k$ . 得到的树称为 border 树. 节点 $x$ 是节点 $y$ 的祖先(允许 $x=y$ ), 当且仅当长度 $x$ 的前缀是长度 $y$ 的前缀的后缀; 当 $x<y$ 时它就是一个真 border. 两个前缀节点的 LCA 对应它们最长的公共 border, 允许答案等于较短前缀.

#h(2em) 若每个非根节点初值为 $1$ , 再按长度从大到小把计数累加给父亲, 则节点 $k$ 的子树和就是长度 $k$ 的前缀在原串中的出现次数. 原因是前缀长度 $k$ 在位置 $i-1$ 结束, 当且仅当节点 $k$ 是前缀长度 $i$ 的祖先.

=== 周期与循环节

#h(2em) 若对所有 $p<=i<n$ 都有 $S_i=S_(i-p)$ , 则 $p$ 是 $S$ 的周期. 周期与 border 满足
$
  p " 是周期 " arrow.l.r.double n-p " 是 border".
$
因此全部周期可由全部 border 得到, 最小周期为 $p_min=n-B_n$ . 周期不要求完整铺满字符串; 若要求原串由某个短串重复若干次得到, 还须满足 $n mod p_min=0$ , 否则最短循环节长度为 $n$ . 例如 `ababa` 的最小周期为 $2$ , 但不存在非平凡的完整循环节.

=== exKMP 中的判定

#h(2em) 令 $Z_i$ 为原串与后缀 `S[i..]` 的 LCP. 对 $0<k<n$ , 长度 $k$ 是 border 当且仅当 $Z_(n-k)=k$ ; $p$ 是周期当且仅当 $Z_p>=n-p$ . 长度 $k$ 的前缀在整串中的出现次数为
$
  1+|{i>0:Z_i>=k}|.
$
因此可对所有 $Z_i$ 的值计数并求后缀和, 在线性时间得到每个前缀的出现次数.

=== Fine-Wilf 周期定理

#h(2em) 若长度为 $n$ 的字符串同时具有周期 $p,q$ , 且
$
  n>=p+q-gcd(p,q),
$
则 $gcd(p,q)$ 也是它的周期. 长度条件保证两个周期产生的相等关系能够连接起来; 条件不足时结论不一定成立. 该定理常用于证明重叠串的周期兼容性, 以及处理重复子串与 runs.
