#import "../common.typ": *

= 网络流 / Flow

== 算法

=== 网络最大流

#code-info(
  [`dinic(n,s,t)`/`dinic_init(n,s,t)` 初始化; `addedge(u,v,cap)` 加有向容量边; `solve()` 返回最大流并保留残量网络. ],
  [一般图时间 $O(n^2m)$ , 空间 $O(n+m)$ ; 容量须非负, 为保持上述复杂度应使 `inf` 不小于最大流. ],
)
#code-file("code/graph/dinic.cpp")

=== 最小费用最大流

#code-info(
  [`ssp(n,s,t)`/`ssp_init(n,s,t)` 初始化; `addedge(u,v,cap,cost)` 加有向容量边及单位费用; `solve()` 返回 `{最大流,最小费用}` 并保留残量网络. ],
  [整数容量下最坏时间 $O(F n m)$ ($F$ 为最大流), 空间 $O(n+m)$ ; 容量须非负, 残量网络不得含可达负费用环, 最短路、总流量及总费用均不得越界. ],
)
#code-file("code/graph/mcmf.cpp")

==== 应用: 最小费用可行流

#h(2em) 在最小费用最大流中将循环的退出条件改为 cost $>=$ 0 即可.

==== 应用: 最大费用最大流

#h(2em) 将边权全部乘以 $-1$ 即可.

=== 上下界网络流

*约定*: 设边 $(u,v)$ 的流量下界和上界分别为 $b(u,v),c(u,v)$ , 则合法流满足 $b(u,v)<=f(u,v)<=c(u,v)$ , 并且除源汇外各点流量守恒.

==== 无源汇上下界可行流

#h(2em) 先令每条边 $(u,v)$ 流过 $b(u,v)$ , 并将其容量改为 $c(u,v)-b(u,v)$ . 令
$
  Delta(v):=sum_((u,v) in E)b(u,v)-sum_((v,w) in E)b(v,w),
$
若 $Delta(v)>0$ , 加边 $(S',v,Delta(v))$ ; 若 $Delta(v)<0$ , 加边 $(v,T',-Delta(v))$ . 原网络存在可行流当且仅当 $S'$ 到 $T'$ 的最大流等于 $sum_(Delta(v)>0)Delta(v)$ ; 此时原边流量为下界加上新图中对应边的流量.

==== 有源汇上下界可行流

#h(2em) 设源汇为 $S,T$ , 加入下界为 $0$ , 上界为 $inf$ 的人工边 $T->S$ , 再求无源汇上下界可行流. 若有解, 记人工边的流量为 $f_0$ , 它就是当前 $S$ 到 $T$ 的流量. 后续保留当前残量网络, 并删除与 $S',T'$ 相连的边和人工边 $T->S$ 的正反残量边.



==== 有源汇上下界最大流

#h(2em) 求出可行流并删除附加边后, 在残量网络中求 $S$ 到 $T$ 的最大流 $Delta f$ , 则最大流的答案为 $f_0+Delta f$ .
#ps[
  这里的附加边是指为求可行流临时加入的 $S'->v,v->T'$ 和人工边 $T->S$ , 包括它们的反向残量边. 求出可行流后应将这些边全部删除, 只保留原图各边的残量网络. 特别地, 人工边流过 $f_0$ 后会产生容量为 $f_0$ 的反向边 $S->T$ ; 若不删除, 第二次最大流会把撤销人工边的流量误当成新增流量. 原图边的反向残量边仍用于调整可行流, 不能删除.
]

==== 有源汇上下界最小流

#h(2em) 求出可行流并删除附加边后, 在残量网络中求 $T$ 到 $S$ 的最大流 $Delta f$ , 则最小流的答案为 $f_0-Delta f$ .



== 常见建模

=== 最小割问题

#h(2em) 对原图每条容量为 $c$ 的有向边直接建容量边, 运行 $s$ 到 $t$ 的最大流, 流量即最小割容量. 结束后从 $s$ 沿正残量边可达的点构成源集, 其余点构成汇集. 若原边是容量为 $c$ 的无向边, 应建两条容量均为 $c$ 的独立有向边, 而不是一条有向边及其零容量残量边.

#h(2em) 对二选一模型, 约定点在源侧表示选 $A$ , 在汇侧表示选 $B$ . 若点 $i$ 选 $A,B$ 的代价分别为 $a_i,b_i$ , 则建 $(i,t,a_i)$ 与 $(s,i,b_i)$ ; 若 $i,j$ 异侧产生代价 $w$ , 则建 $(i,j,w)$ 与 $(j,i,w)$ 两条独立边. 跑最大流, 其值即最小代价.

=== 最大权闭合子图

#h(2em) 给每个点 $v$ 一个可正可负的权 $w_v$ , 并用原图边 $u->v$ 表示选 $u$ 必须选 $v$ . 若 $w_v>0$ , 建 $(s,v,w_v)$ ; 若 $w_v<0$ , 建 $(v,t,-w_v)$ ; 对每个依赖 $u->v$ 建 $(u,v,inf)$ . 令 $P=sum_v max(w_v,0)$ , 跑最大流 $F$ 后答案为 $P-F$ , 残量网络中从 $s$ 可达的原图点就是所选点. 要求 $inf$ 大于所有有限容量之和.

=== 点容量与不相交路径

#h(2em) 点 $v$ 的流量上限为 $c_v$ 时, 将其拆成 $v_("in")$ 与 $v_("out")$ , 建 $(v_("in"),v_("out"),c_v)$ ; 所有原入边连向 $v_("in")$ , 原出边从 $v_("out")$ 连出. 求最小点割时令 $c_v$ 为删除 $v$ 的代价, 不允许删除的点取 $inf$ .

#h(2em) 求最多边不相交的 $s-t$ 路径时令每条原边容量为 $1$ ; 求内部点不相交路径时将每个内部点拆点并令中间边容量为 $1$ , 原边容量按题意设置. 跑最大流, 流量即最多路径数; 若还要求路径代价最小, 则发送指定流量的最小费用流.

=== 二分图匹配与多重匹配

#h(2em) 对二分图 $(L,R,E)$ , 建 $s->L->R->t$ . 标准匹配中所有边容量均为 $1$ ; 多重匹配中令 $(s,u)$ 与 $(v,t)$ 的容量分别为两侧点的可用次数, 中间边容量为该关系的可用次数. 跑最大流, 流量即最大匹配数, 中间边的流量给出具体匹配.

=== DAG 最小路径覆盖

#h(2em) 对每个原图点 $v$ 建左副本 $v_L$ 与右副本 $v_R$ , 建 $(s,v_L,1)$ 与 $(v_R,t,1)$ ; 每条原边 $u->v$ 建 $(u_L,v_R,1)$ . 若最大流为 $F$ , 则点不相交的最小路径覆盖数为 $n-F$ , 满流的中间边给出路径中的相邻关系. 该结论要求原图为 DAG ; 若路径可交且只要求覆盖所有点, 先对 DAG 求传递闭包再作上述建图.

=== 二分图点覆盖与独立集

#h(2em) 每个点 $v$ 有非负权 $w_v$ . 对 $u in L$ 建 $(s,u,w_u)$ , 对 $v in R$ 建 $(v,t,w_v)$ , 对原图每条边 $u v$ 建 $(u,v,inf)$ . 跑最大流 $F$ , 则 $F$ 是最小权点覆盖, $sum_v w_v-F$ 是最大权独立集. 若记残量网络中从 $s$ 可达的原图点集为 $X$ , 则最小点覆盖由左部不可达点与右部可达点组成, 最大独立集取其补集.

=== 分配与运输

#h(2em) 对供应点 $i$ 建 $(s,i,a_i,0)$ , 对需求点 $j$ 建 $(j,t,b_j,0)$ , 对允许的分配建 $(i,j,c_(i,j),w_(i,j))$ , 其中 $a_i,b_j$ 是供需量, $c_(i,j)$ 是最大运送量, $w_(i,j)$ 是单位代价. 跑最小费用最大流; 流量等于总需求时方案可行, 费用即最小总代价. 所有供需量均为 $1$ 时就是带权二分图分配; 求最大收益时将收益取负作为费用.

=== 常见费用流收益模型

#h(2em) 若一条边可经过多次, 但收益 $w$ 最多获得一次, 则并联容量为 $1$ , 费用为 $-w$ 的边与容量为 $inf$ , 费用为 $0$ 的边. 对点的一次性收益可先拆点再作同样处理. 按题意发送指定流量的最小费用流.

#h(2em) 求最大权的区间集且任意位置最多被 $k$ 个区间覆盖时, 先离散化端点, 在相邻坐标间建容量为 $k$ , 费用为 $0$ 的正向边, 对区间 $[l_i,r_i]$ 建 $(l_i,r_i,1,-w_i)$ . 从最左端到最右端发送恰好 $k$ 单位的最小费用流, 答案为费用的相反数. 端点相交是否算重叠应在离散化时统一处理.

=== 时间展开网络

#h(2em) 给定时间上界 $T$ , 对每个原图点 $v$ 和时刻 $0<=tau<=T$ 建状态点 $(v,tau)$ . 原边 $u->v$ 若需时 $d$ , 则建 $(u,tau)->(v,tau+d)$ ; 允许等待时建 $(v,tau)->(v,tau+1)$ , 容量分别表示每时刻的通行量与停留量. 将初始供给连向第 $0$ 层, 目标状态连向汇点, 跑最大流判定可行性或最大完成量, 跑费用流优化总代价; 最小完成时间可二分 $T$ 或逐层加点. 图规模与 $T$ 成正比.

=== 参数化最小割

#h(2em) 以无向图最大密度子图为例, 设原图有 $n$ 个点, $m$ 条边, 判定是否存在非空点集 $U$ 满足 $abs(E(U))/abs(U)>lambda$ . 对每个点 $v$ 建 $(s,v,m)$ 与 $(v,t,m+2lambda-deg(v))$ , 每条无向边替换为两条容量为 $1$ 的独立有向边. 最小割小于 $m n$ 当且仅当判定成立, 此时源侧原图点给出一个可行点集. 对 $lambda$ 二分即可; 使用整数最大流时应先将所有容量同乘分母.

== 模拟费用流(反悔贪心)

=== 流理论
